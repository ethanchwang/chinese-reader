version: 1.0
runtime: python3 
build:
  commands:
    build:
      - pip install -r requirements.txt
run: 
  network:
    port: 8080
  command: gunicorn app:app --bind 0.0.0.0:8080 --log-level info --access-logfile - --error-logfile - --capture-output